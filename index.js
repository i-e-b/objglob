"use strict";
var global, exports;


(function (provides) {

    provides.filter = function filter(patterns, input){
        return input;
    };
/* istanbul ignore next */ // `this` branch doesn't get followed
})(global || exports || this);
